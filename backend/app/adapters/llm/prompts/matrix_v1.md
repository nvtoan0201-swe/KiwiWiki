# Comparison matrix — one cluster row

Fill the comparison-matrix row for one cluster, for the research question:

> {research_question}

## Cluster: {cluster_label}

{cluster_description}

### Member papers

{members}

## Dimensions

{dimensions}

For each dimension (by `dimension_index`), say how THIS cluster sits on it in
`summary`, citing the member papers that ground the cell (`source_indexes`,
roster indexes as given) and a supporting `passage` (≤15-word quote or
paraphrase + locator) from one of them. Give each cell an honest
`confidence_label`.

If the cluster's papers genuinely do not speak to a dimension, set
`empty=true` for that cell instead of inventing a position.
