# Cluster naming & characterization

The analyzed literature for the research question

> {research_question}

has been grouped by embedding similarity into the candidate clusters below.
For each cluster, give:

- `label`: a short name for the approach / school of thought it represents;
- `description`: one or two sentences on what unites these papers;
- `defining_characteristics`: the assumptions, methods, or framings that
  define membership;
- `bridging_source_indexes`: roster indexes of papers that are primarily in
  this cluster but clearly also bridge to another one.

Characterize what the papers actually share — do not force a narrative the
members don't support. Use each cluster's `cluster_index` and the papers'
roster indexes exactly as given.

## Candidate clusters

{clusters}
